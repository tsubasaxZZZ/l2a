machine:
  python:
    version: 3.5.0

dependencies:
  pre:
    - pip install -r ./requirements.txt
    - python ./index.py
      	     background: true

test:
  override:
    - pwd ~
    - ifconfig
    - echo HelloWorld
    - python --version
    - curl http://127.0.0.1/hello
    - exit 0